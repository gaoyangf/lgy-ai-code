spring:
  application:
    name: lgy-ai-code
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lgy_ai_code
    username: root
    password: gy123456
server:
  port: 9527
  servlet:
    context-path: /api #?????

# springdoc-openapi????
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.lgy.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn

